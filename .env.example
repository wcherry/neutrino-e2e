# GitHub Container Registry owner (used when pulling images instead of building locally)
# Defaults to "williamcherry" if not set
GHCR_OWNER=wcherry

# Override where run artifacts are stored (default: /tmp/neutrino-e2e/<run-id>)
# NEUTRINO_E2E_BASE_DIR=/tmp/neutrino-e2e
