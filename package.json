{
  "name": "neutrino-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "./scripts/run-tests.sh",
    "test:skip-build": "./scripts/run-tests.sh --skip-build",
    "playwright": "npx playwright test",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
